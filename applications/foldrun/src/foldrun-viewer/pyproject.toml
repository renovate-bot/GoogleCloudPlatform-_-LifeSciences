[project]
name = "foldrun-viewer"
version = "0.1.0"
description = "FoldRun Structure Viewer"
requires-python = ">=3.13"
dependencies = [
    "Flask==3.1.3",
    "google-cloud-storage==3.9.0",
    "gunicorn==26.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
